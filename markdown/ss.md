阅读src/py00代码，当前代码下载的股票数据是基于当前在市的股票列表，因此对于已经退市的股票（如1998年上市、2004年退市的股票）不会包含在下载的记录中。这导致在股票回测时产生了偏差  ，提前过滤了退市股。

我现在想实现爬取的完整历史交易记录，包含退市股的信息。每一行数据应该在现有基础上，新增列表示今日是否是ST（isST）、今日是否交易（isTrading）。  所有修改需要新建一个副本后在副本上修改。  

请先制定详细修改计划，等我批准前不要执行



阅读src下的代码，有别的模型认为，当前的历史日k数据不包含历史退市的股票，导致在回测时会高估收益，因为退市股往往有很强的负面收益；然而，回测代码中似乎直接排除了ST股从而不交易，所以这种影响到底大不大？是否还有必要爬取全量的历史日K数据？